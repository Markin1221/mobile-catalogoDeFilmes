import { StatusTentativaPipe } from './status-tentativa-pipe';

describe('StatusTentativaPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTentativaPipe();
    expect(pipe).toBeTruthy();
  });
});
