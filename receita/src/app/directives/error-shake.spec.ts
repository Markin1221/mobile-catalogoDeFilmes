import { ErrorShakeDirective } from './error-shake';

describe('ErrorShakeDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorShakeDirective({} as any, {} as any);
    expect(directive).toBeTruthy();
  });
});
